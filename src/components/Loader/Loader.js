import React from "react";
import "../../assets/styles/loader.scss";

/**
 * Functional component to display loader.
 */
export const Loader = () => {
    return (
        <div className="loader-container">
            <div className="loader" />
        </div>
    );
}